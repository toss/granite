# @granite-js/plugin-router

A Route Generator for Granite project

## Next Plugin API

The `/next` export provides the router plugin as a `PluginOption`.

```ts
import { router } from '@granite-js/plugin-router/next';

const plugins = [router()];
```
