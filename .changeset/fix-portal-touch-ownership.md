---
'@granite-js/micro-frontend': patch
---

Prevent iOS Portal hosts from dispatching duplicate Fabric touch events when another React Native root owns the touched view. Preserve touch handling for plain hosted content and nested Portal hosts.
