---
'@granite-js/plugin-micro-frontend': patch
'@granite-js/react-native': patch
---

Add generic service-bundle loading primitives for evaluating independently built services in one JavaScript runtime. Allow applications mounted in that runtime to opt into an independent navigation tree and initialize routing from their own native props.
